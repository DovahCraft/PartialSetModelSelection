#travis configuration for coverage testing
language: cpp
compiler: gcc

script:
  -g++ -g -o partialSelect PartialModelSelectionMain.cpp PartialModelSelection.cpp -pthread -lgtest
  -./partialSelect

